{% extends 'base.html.twig' %}

{% block body -%}
{% for file in files %}
{{ dump(file)}}
<a href="{{ uploaded_asset(file.basename) }}">
<img src="{{ file.path | imagine_filter('full') }}" alt="" style="padding:10px;"></a>
   <br> {{ file.path }}<br>
{% endfor %}
 {% endblock %}